/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // This should not affect any implications in productions, and helps with debugging.
};

module.exports = nextConfig;
